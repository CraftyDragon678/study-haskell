parser는 raw data(String) 을 받아서, 부가적인 데이터(a)를 추가하고 본래 타입(String)을 더해서 돌려줌

(+++)은 파서를 합성해 줌
이전에 만든 (+++)은 실패하면 다음 것을 실행하는 구조였지만, 다양한 파서가 나올 수 있음

부가적인 정보를 만들어 주는 함수(파서) 와 합성(+++)
그리고, 이 파서가 모나드임 ~~두둥~~

